<% let _inc_link_url = 'javascript:void(0);' %><% let _inc_external = false %><% if ((link.url || '').indexOf('http') === 0) { %><% _inc_link_url = `${link.url}?${utm}` %><% _inc_external = true %><% } else if (link.url) { %><% _inc_link_url = `${config.root || '/'}${link.url[0] === '/' ? link.url.slice(1) : link.url}` %><% } %><a href="<%= _inc_link_url %>"<% if (_inc_external) { %> target="_blank" rel="external nofollow"<% } %>><% if (wrap === true) { %><span><%= link.text %></span><% } else { %><%= link.text %><% } %></a>
